# Environment Configuration
ENVIRONMENT=development

# API Authentication
API_KEY=dev-key-12345

# Production AWS/S3 Settings (not needed for development mode)
AWS_ACCESS_KEY_ID=your_access_key
AWS_SECRET_ACCESS_KEY=your_secret_key
AWS_DEFAULT_REGION=us-east-1

# Email Settings (optional)
MAILJET_API_KEY=your_mailjet_key
MAILJET_API_SECRET=your_mailjet_secret
EMAIL_FROM=noreply@yourapp.com
EMAIL_FROM_NAME=Cleft Care API

# Server Configuration
PORT=8080

# Development Mode Instructions:
# 1. Set ENVIRONMENT=development to use local audio files
# 2. Place audio files in ./audios/ directory
# 3. Use any API_KEY for testing (e.g., "dev-key-12345")
# 4. AWS credentials not required in development mode